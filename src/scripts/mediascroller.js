export default class MediaScroller {
    constructor() {
        // Create list of player names that we need to add as the href
        this.players = ['mookie', 'tatis', 'trout', 'seager', 'machado', 'harper', 'acuna', 'altuve', 'freeman', 'jram', 'judge', 'ohtani', 'soto'];

        
    }
}